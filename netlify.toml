[build]
  # Static site - no build needed
  base = "frontend"
  command = "echo 'Static content deployed'"
  publish = "."

# Only redirect 404s to index.html for SPA-style deep links
# NOT all routes (since we have real HTML pages)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404

# Environment variables for production
[context.production]
  environment = { API_URL = "https://cgs-attendance-system.onrender.com" }

# Cache control headers
[[headers]]
  for = "*.html"
  [headers.values]
    Cache-Control = "max-age=0, must-revalidate"

[[headers]]
  for = "*.js"
  [headers.values]
    Cache-Control = "max-age=31536000, immutable"

[[headers]]
  for = "*.css"
  [headers.values]
    Cache-Control = "max-age=31536000, immutable"
